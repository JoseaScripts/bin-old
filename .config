#!/bin/bash
# archivo oculto de muestra
# comprobando si se añade al proyecto.
# voy a subir el repositorio con
git add -all
git commit -m "v0.1"
git rm *.conf
git remote add origin https://github.com/JoseaScripts/bin
git push -u origin master
